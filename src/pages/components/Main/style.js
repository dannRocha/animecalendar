import styled from 'styled-components'


const Main = styled.main`
    
    background: var(--_1-color);
    width: 100%;
    overflow-y: auto;
    
    header {
        background: var(--_1-color);
        text-align: center;
        font-size: 30pt;
        color: #FFF;
    }
`

export default Main